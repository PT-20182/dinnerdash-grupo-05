<%= form_with model: @meal_category, local: true do |f| %>
  <%= f.label "Nome", class: "form-legenda" %> <br>
  <%= f.text_field :name, class: "input-text" %>
  <%= f.submit "Criar Categoria", class: "submit"%>
<% end %>