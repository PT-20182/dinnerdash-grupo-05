<h2>Nova categoria de refeição</h2>
<br>
<%= form_with model: @meal_category, local: true do |f| %>
  <%= f.label "Nome" %>
  <%= f.text_field :name %>
  <%= f.submit "Inserir refeição", class: "classe"%>
<% end %>