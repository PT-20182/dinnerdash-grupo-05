<div class="backoffice-main-container">
  <!-- imagem superior -->
  <%= render "superior_image" %>

  <!-- menu seletor -->
  <%= render "selector_menu" %>

  <!-- area do form -->
  <div class="table-area">
    <div class="category-form">
      <h5>Nova categoria de refeição</h5>
      <hr class="hr-divider">
      <%= render "category_form" %>
    </div>
  </div>
</div>