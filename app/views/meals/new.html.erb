<div class="backoffice-main-container">

  <!-- imagem superior -->
  <%= render "top_banner/banner", is_backoffice: true, title: "Backoffice", subtitle: "Gerencie pedidos, refeições e mais." %>

  <!-- menu seletor -->
  <%= render "backoffice/selector_menu" %>

  <%# area do form %>
  <div class="table-area">
    <div class="backoffice-form" id="meal-form">
      <h5>Nova Refeição</h5>
      <hr class="hr-divider">
      <%= render 'form' %>
    </div>
  </div>
</div>
